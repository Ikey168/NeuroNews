apiVersion: v1
kind: Service
metadata:
  name: nginx-api-proxy-service
  namespace: neuronews
  labels:
    app: nginx-api-proxy
spec:
  type: LoadBalancer
  selector:
    app: nginx-api-proxy
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
