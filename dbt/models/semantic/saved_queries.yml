version: 2

saved_queries:
  - name: newsroom_daily
    description: "Key daily metrics for newsroom dashboard"
    query_params:
      metrics:
        - articles_ingested
        - unique_sources  
        - avg_article_sentiment
      group_by:
        - TimeDimension('metric_time', 'day')
        - Dimension('source__country')
