version: 2

tests:
  - name: test_entity_positions_logical
    description: "Ensures entity end_position is always greater than start_position"
    config:
      tags: ['dq']
      
  - name: test_confidence_score_range
    description: "Ensures all confidence scores are between 0.0 and 1.0"
    config:
      tags: ['dq']
      
  - name: test_url_format_validity
    description: "Ensures all URLs follow proper HTTP/HTTPS format"
    config:
      tags: ['dq']
      
  - name: test_fact_source_integrity
    description: "Ensures all fact_news records have valid source_key references"
    config:
      tags: ['dq']
